
package ${packageName}.service;

import ${packageName}.entity.${upperCase(module.name)};

/**
 * Service - ${module.memo}
 * 
 * @author ${author}
 * @version ${version}
 */
public interface ${upperCase(module.name)}Service extends BaseService<${upperCase(module.name)}, Long> {

}