
package ${packageName}.dao;

import ${packageName}.entity.${upperCase(module.name)};

/**
 * Dao - ${module.memo}
 * 
 * @author ${author}
 * @version ${version}
 */
public interface ${upperCase(module.name)}Dao extends BaseDao<${upperCase(module.name)}, Long> {

}